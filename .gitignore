# Secrets — never commit
.env
*.local

# AI agent instructions — not for the public repo
CLAUDE.md

# Internal project notes — kept locally, not published
docs/

# Notebooks — kept locally as working artifacts, not in the public repo
notebooks/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.ipynb_checkpoints/

# Jupyter
.jupyter/

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/

# Data — raw is small and worth tracking; processed and final are
# regenerated by scripts/01_clean_delay_data.py and don't belong in git.
data/processed/
data/final/

# Power BI — keep .pbix but not its temp/cache files
*.pbix.tmp
